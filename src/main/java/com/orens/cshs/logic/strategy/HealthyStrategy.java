package com.orens.cshs.logic.strategy;

public class HealthyStrategy implements IStrategy {

    @Override
    public void step() {
        // move to random location (need access to distance, contagiousRadius, isolationTime)
        //


    }
}
